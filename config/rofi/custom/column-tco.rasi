configuration {
    font:                "SpaceMono Nerd Font 14";
    show-icons:          true;
    icon-theme:          "Papirus-Dark";
    display-drun:        "";
    drun-display-format: "{name}";
    disable-history:     false;
    fullscreen:          false;
    hide-scrollbar:      true;
    sidebar-mode:        false;
}

* {
    c-teal:        #94E2D5;
    c-selected-bg: #313244;

    /* Full transparent par défaut */
    background-color: transparent;
    text-color:       @c-teal;
}

window {
    transparency:     "real";

    /* FULL transparent (pas de bloc noir / blur qui accroche) */
    background-color: transparent;

    /* Bordure "existante" mais invisible */
    border:           2px;
    border-color:     transparent;
    border-radius:    0px 40px 40px 0px;

    /* Anti faux-contour */
    box-shadow:       0px 0px 0px 0px;

    location:         west;
    anchor:           west;
    height:           96%;
    y-offset:         0%;
    width:            110px;
}

mainbox {
    children: [ inputbar, listview ];
    spacing:  0px;
    padding:  20px 10px;
}

inputbar {
    children: [ entry ];
    expand:   false;
    padding:  0px;
    height:   0px;
    margin:   0px;
}

entry {
    placeholder:      "";
    text-color:       transparent;
    cursor-color:     transparent;
}

listview {
    columns: 1;
    lines:   9;
    spacing: 15px;
    cycle:   false;
    dynamic: true;
    layout:  vertical;
    padding: 20px 0px 0px 0px;
}

element {
    orientation:   vertical;
    border-radius: 100%;
    padding:       15px 0px;
    cursor:        pointer;
    margin:        0px 5px;

    /* Bordure "existante" mais invisible */
    border:        2px;
    border-color:  transparent;

    /* Anti faux-contour */
    box-shadow:    0px 0px 0px 0px;
}

element-text {
    enabled: false;
}

element-icon {
    size:             48px;
    horizontal-align: 0.5;
    cursor:           inherit;
}

element selected {
    background-color: @c-selected-bg;

    /* IMPORTANT : on laisse la bordure invisible même en selected */
    border-color:     @c-teal;

    /* au cas où */
    box-shadow:       0px 0px 0px 0px;
}
